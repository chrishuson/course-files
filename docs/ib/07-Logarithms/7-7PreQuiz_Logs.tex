\documentclass[12pt, twoside]{article}
\input{../preamble}

\fancyhead[LO]{La Scuola d'Italia / Dr.\ Huson / IB Math \\* 7 May 2026}
\fancyhead[RO]{\fbox{\begin{minipage}{6cm}\footnotesize First \& Last name:\\[0.5cm]\end{minipage}}}
\fancyfoot[C]{\thepage}

\begin{document}
\subsubsection*{7.7 Pre-Quiz: Logarithms (no calculator)}

\begin{enumerate}[itemsep=0.15cm]

%--- Problem 1: AA SL 25M P1 TZ1 Q1 (qb_id 25M.1.SL.TZ1.1) — express in form ln k ---
\item[1.] Write each of the following expressions in the form $\ln k$,
where $k \in \mathbb{Z}^{+}$.

\begin{enumerate}[itemsep=0.15cm]
  \item $\ln 3 + \ln 4$

  \item $3\ln 2$

  \item $-\ln\dfrac{1}{2}$
\end{enumerate}

\begin{tikzpicture}
  \draw (-0.6,0) rectangle (11,14);
  \foreach \y in {1,2,...,13} \draw[dotted] (0,\y)--(10.5,\y);
\end{tikzpicture}

\newpage

%--- Problem 2: AA SL 25M P1 TZ3 Q2 (qb_id 25M.1.SL.TZ3.2) — log_{10} expressions in p, q ---
\item[2.] Let $\log_{10} 2 = p$ and $\log_{10} 3 = q$.

\begin{enumerate}[itemsep=0.15cm]
  \item Find an expression for $\log_{10} 24$ in terms of $p$ and $q$.

  \item Find an expression for $\log_{3} 8$ in terms of $p$ and $q$.
\end{enumerate}

\begin{tikzpicture}
  \draw (-0.6,0) rectangle (11,14);
  \foreach \y in {1,2,...,13} \draw[dotted] (0,\y)--(10.5,\y);
\end{tikzpicture}

\newpage

%--- Problem 3: AA SL 24M P1 TZ1 Q3 (qb_id 24M.1.SL.TZ1.3) — log_{10} a = 1/3 ---
\item[3.] It is given that $\log_{10} a = \dfrac{1}{3}$, where $a > 0$.

Find the value of

\begin{enumerate}[itemsep=0.15cm]
  \item $\log_{10}\!\left(\dfrac{1}{a}\right)$;

  \item $\log_{1000} a$.
\end{enumerate}

\begin{tikzpicture}
  \draw (-0.6,0) rectangle (11,14);
  \foreach \y in {1,2,...,13} \draw[dotted] (0,\y)--(10.5,\y);
\end{tikzpicture}

\newpage

%--- Problem 4: AA SL Exemplar P1 Q2 (qb_id EXN.1.SL.TZ0.2) — solve 2 ln x = ln 9 + 4 ---
\item[4.] Solve the equation $2\ln x = \ln 9 + 4$. Give your answer in
the form $x = p\, e^{q}$ where $p, q \in \mathbb{Z}^{+}$.

\begin{tikzpicture}
  \draw (-0.6,0) rectangle (11,17);
  \foreach \y in {1,2,...,16} \draw[dotted] (0,\y)--(10.5,\y);
\end{tikzpicture}

\newpage

%--- Problem 5: AA SL 25N P1 TZ3 Q6 (qb_id 25N.1.SL.TZ3.6) — solve 3 log_8(10x) - log_4 x = 1 ---
\item[5.] Solve the equation
$3\log_{8}(10x) - \log_{4} x = 1$, \;for $x > 0$.

\begin{tikzpicture}
  \draw (-0.6,0) rectangle (11,17);
  \foreach \y in {1,2,...,16} \draw[dotted] (0,\y)--(10.5,\y);
\end{tikzpicture}

\newpage

%--- Problem 6: AA SL 24N P1 TZ1 Q8 (qb_id 24N.1.SL.TZ1.8) — f(x)=log_2(8x), inverse, transformations ---
\item[6.] The function $f$ is defined as $f(x) = \log_{2}(8x)$, where
$x > 0$.

\begin{enumerate}[itemsep=0.15cm]
  \item[(a)] Find the value of
  \begin{enumerate}[label=(\roman*), itemsep=0.15cm]
    \item $f(2)$;

    \item $f\!\left(\dfrac{1}{8}\right)$.
  \end{enumerate}

  \item[(b)] Find an expression for $f^{-1}(x)$.

  \item[(c)] Hence, or otherwise, find $f^{-1}(0)$.
\end{enumerate}

\begin{tikzpicture}
  \draw (-0.6,0) rectangle (11,13);
  \foreach \y in {1,2,...,12} \draw[dotted] (0,\y)--(10.5,\y);
\end{tikzpicture}

\newpage

The function $f$ is defined as $f(x) = \log_{2}(8x)$, where $x > 0$.

The graph of $y = f(4x^{2})$ can be obtained by translating and
stretching the graph of $y = \log_{2} x$.

\begin{enumerate}[itemsep=0.15cm]
  \item[(d)] Describe these two transformations specifying the order
  in which they are to be applied.
\end{enumerate}

\begin{tikzpicture}
  \draw (-0.6,0) rectangle (11,16);
  \foreach \y in {1,2,...,15} \draw[dotted] (0,\y)--(10.5,\y);
\end{tikzpicture}

\end{enumerate}
\end{document}
